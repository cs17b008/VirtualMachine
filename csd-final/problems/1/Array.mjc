/**
 * Represents an array.
 */
class Array
{
    /** Constructs a new Array of the given size. */
    static Array newobj(int size) {
        return Memory.alloc(size);
    }
    /** Deletes/deallocates this array. */
    void dispose() {
        Memory.deAlloc(this);
        return;
    }
}
